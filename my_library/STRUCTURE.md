# My Library 项目结构

一个带有推门动画效果的图书馆应用，采用 Claude 风格暗色主题设计。

## 📁 目录结构

```
lib/
├── main.dart                    # 应用入口
├── themes/                      # 主题配置
│   └── colors.dart              # 配色方案
├── models/                      # 数据模型
│   ├── book_model.dart          # 书籍模型
│   └── note_model.dart          # 笔记模型
├── utils/                       # 工具函数
│   └── mock_data.dart           # Mock 数据生成
├── widgets/                     # UI 组件
│   ├── book_widget.dart         # 书籍组件
│   └── bookshelf_section.dart   # 书架分层组件
└── pages/                       # 页面
    ├── door_transition_page.dart    # 推门动画过渡页
    ├── second_page.dart             # 主页面（书架展示）
    └── book_detail_page.dart        # 书籍详情页
```

## 📦 模块说明

### `main.dart`
应用入口文件，包含：
- `main()` - 应用启动函数
- `MyApp` - 根组件，配置全局主题

### `themes/`
主题相关配置。

| 文件 | 说明 |
|------|------|
| `colors.dart` | Claude 风格配色方案（Warm Dark Mode），包含背景色、强调色、书脊配色盘等 |

### `models/`
数据模型定义。

| 文件 | 说明 |
|------|------|
| `book_model.dart` | 书籍模型，包含 id、标题、页数、颜色、编辑时间 |
| `note_model.dart` | 笔记模型，包含 id、内容、标签 |

### `utils/`
工具函数。

| 文件 | 说明 |
|------|------|
| `mock_data.dart` | `generateBooks()` 函数，生成随机测试数据 |

### `widgets/`
可复用 UI 组件。

| 文件 | 说明 |
|------|------|
| `book_widget.dart` | 单本书籍展示组件，根据页数计算厚度和高度，点击跳转详情页 |
| `bookshelf_section.dart` | 书架分层组件，显示分类标题和书籍列表 |

### `pages/`
应用页面。

| 文件 | 说明 |
|------|------|
| `door_transition_page.dart` | 欢迎页，带推门动画效果，点击按钮后门向两侧滑开 |
| `second_page.dart` | 主页面，展示多个书架分类，支持拖拽重排书籍 |
| `book_detail_page.dart` | 书籍详情页，左侧信息栏 + 右侧便利贴展示 |

## 🎨 设计特点

- **Claude 风格配色**：暖色调暗色系，焦橙色强调
- **推门动画**：沉浸式入口体验
- **书架式布局**：直观的书籍管理
- **拖拽排序**：灵活调整书籍顺序

## 🚀 运行

```bash
cd my_library
flutter run
```
